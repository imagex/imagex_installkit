<?php
/**
 * @file
 */

/**
 * Implements hook_install().
 */
function imagex_install() {
  // Create and initial default variables.
  foreach (imagex_default_variables() as $variable => $default_value) {
    variable_set($variable, $default_value);
  }
}
